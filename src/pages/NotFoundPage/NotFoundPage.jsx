import css from './NotFoundPage.module.scss';

const NotFoundPage = () => {
  return (
    <section className={css.section}>
      <p>NotFoundPage</p>
    </section>
  );
};

export default NotFoundPage;
